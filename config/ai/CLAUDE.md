# System Context

## Available Tools
Run `aic tools` or read ~/.config/ai/tools.md for available CLI tools.

## Guidelines
Run `aic guidelines` or read ~/.config/ai/guidelines.md for coding standards.

## Composing Context
Use `aic <module> [module...]` to combine context modules.
Available: tools, guidelines, nix, etc.
