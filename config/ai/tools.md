# CLI Tools

rg, fd, sd, jq, yq, miller, jless, gh, git-lfs, just, direnv, hyperfine, tokei, act, pre-commit, xh, socat, mtr, age, gnupg, rsync, rclone, croc, ffmpeg, imagemagick, exiftool

+ GNU coreutils (find, grep, sed, awk, cat, ls, du, df, ps, etc.)
