result
.direnv
flake.lock.backup

# Nebula secrets
secrets/
*.key

# Common credential file names (preventive)
.env
.env.*
id_*
*.pem
*.p12
.netrc
*credential*

.claude

# Generated by the pre-commit-hooks devshell shellHook (points into /nix/store)
.pre-commit-config.yaml

# qBittorrent rewrites this on exit; stable keys are managed via
# xdg.configFile text in modules/home/linux/xdg.nix
config/qBittorrent/qBittorrent.conf
